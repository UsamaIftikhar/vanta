<section
  class='marketing-section'
  style='--section-bg:{{ section.settings.background }};--section-fg:{{ section.settings.foreground }};--space:{{ section.settings.spacing }}px;--text-align:{{ section.settings.alignment }}'
>
  <div class='container'>
    <div class='section-heading' data-animate>
      <div>
        <p class='eyebrow'>{{ section.settings.eyebrow | escape }}</p>
        <h2>{{ section.settings.heading | escape }}</h2>
      </div>
      <div class='rte'>{{ section.settings.text }}</div>
    </div>
    <div
      class='horizontal-story'
      tabindex='0'
      role='region'
      aria-label='{{ section.settings.heading | escape }}'
    >
      {% for block in section.blocks %}
        <article {{ block.shopify_attributes }}>
          <span class='eyebrow'>0{{ forloop.index }}</span>
          {% assign demo_kinds = 'technology,aero,terra' | split: ',' -%}
          {%- assign demo_index = forloop.index0 | modulo: 3 -%}
          {%- render 'image', image: block.settings.image, demo: demo_kinds[demo_index] %}
          <h3>{{ block.settings.heading | escape }}</h3>
          <div class='rte'>{{ block.settings.text }}</div>
          {% if block.settings.link -%}
            <a class='text-link' href='{{ block.settings.link }}'>
              {{- 'general.read_more' | t }} ↗</a
            >
          {%- endif %}
        </article>
      {% endfor %}
    </div>
  </div>
</section>
{% schema %}
{
  "name": "Horizontal story",
  "settings": [
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow",
      "default": "VANTA Athletics"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Built for motion."
    },
    {
      "type": "richtext",
      "id": "text",
      "label": "Text",
      "default": "<p>Engineered for movement. Designed for everywhere.</p>"
    },
    {
      "type": "color",
      "id": "background",
      "label": "Background",
      "default": "#101110"
    },
    {
      "type": "color",
      "id": "foreground",
      "label": "Text color",
      "default": "#f3f2ed"
    },
    {
      "type": "select",
      "id": "alignment",
      "label": "Text alignment",
      "options": [
        {
          "value": "left",
          "label": "Left"
        },
        {
          "value": "center",
          "label": "Center"
        }
      ],
      "default": "left"
    },
    {
      "type": "range",
      "id": "spacing",
      "label": "Vertical spacing",
      "min": 24,
      "max": 160,
      "step": 8,
      "unit": "px",
      "default": 96
    }
  ],
  "blocks": [
    {
      "type": "frame",
      "name": "Story frame",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        },
        {
          "type": "text",
          "id": "heading",
          "label": "Heading",
          "default": "Explore new ground."
        },
        {
          "type": "richtext",
          "id": "text",
          "label": "Text",
          "default": "<p>A different perspective on everyday movement.</p>"
        },
        {
          "type": "url",
          "id": "link",
          "label": "Link"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Horizontal story"
    }
  ]
}
{% endschema %}
